CREATE TABLE PAYABLE_ATTENDANCE_OUTCOMES (
  PAYABLE_ATTENDANCE_OUTCOMES_ID NUMBER(10),
  EVENT_TYPE                     VARCHAR2(12)                      NOT NULL,
  OUTCOME_CODE                   VARCHAR2(12)                      NOT NULL,
  PAY_FLAG                       VARCHAR2(1)  DEFAULT 'N',
  AUTHORISED_ABSENCE_FLAG        VARCHAR2(1)  DEFAULT 'N'    ,
  START_DATE                     DATE                                 NOT NULL,
  END_DATE                       DATE,
  CREATE_DATETIME                TIMESTAMP(9) DEFAULT SYSTIMESTAMP NOT NULL,
  CREATE_USER_ID                 VARCHAR2(32) DEFAULT USER         NOT NULL,
  MODIFY_DATETIME                TIMESTAMP(9),
  MODIFY_USER_ID                 VARCHAR2(32),
  AUDIT_TIMESTAMP                TIMESTAMP(9),
  AUDIT_USER_ID                  VARCHAR2(32),
  AUDIT_MODULE_NAME              VARCHAR2(65),
  AUDIT_CLIENT_USER_ID           VARCHAR2(64),
  AUDIT_CLIENT_IP_ADDRESS        VARCHAR2(39),
  AUDIT_CLIENT_WORKSTATION_NAME  VARCHAR2(64),
  AUDIT_ADDITIONAL_INFO          VARCHAR2(256),
  CONSTRAINT PAYABLE_ATTENDANCE_OUTCOMES_PK PRIMARY KEY (PAYABLE_ATTENDANCE_OUTCOMES_ID),
  CONSTRAINT PAYABLE_ATT_OUTCOMES_UK1 UNIQUE (EVENT_TYPE, OUTCOME_CODE, START_DATE)
);
