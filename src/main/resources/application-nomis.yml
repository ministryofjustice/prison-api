spring:
  datasource:
    hikari:
      maximum-pool-size: 10
      connectionTimeout: 2500
      validationTimeout: 500

  replica:
    datasource:
      hikari:
        maximum-pool-size: 10
        connectionTimeout: 2000
        validationTimeout: 500
