package uk.gov.justice.hmpps.prison.repository.jpa.model;

/**
 * In NOMIS this is a reference code however an enum will suffice given there are only two possible (simple) values.
 */
public enum MovementDirection {
    IN, OUT
}
